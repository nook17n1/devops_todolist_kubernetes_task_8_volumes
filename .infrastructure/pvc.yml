apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
  namespace: todoapp
spec:
  resources:
    requests:
      storage: 1Gi
  volumeName: mypv
  accessModes:
    - ReadWriteMany
  storageClassName: standard
